@{
    ViewData["Title"] = "Import Matieres";
}

<h1>@ViewData["Title"]</h1>

<h4>Upload a CSV file with columns: <strong>NomMatiere, CodeMatiere</strong></h4>
<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Upload" method="post" enctype="multipart/form-data">
            <div class="form-group mb-3">
                <label for="file" class="control-label">CSV File</label>
                <input name="file" type="file" class="form-control" accept=".csv" required />
            </div>
            <div class="form-group">
                <input type="submit" value="Upload" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</div>
